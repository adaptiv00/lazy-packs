stack                 = monitoring
docker_image          = tchiotludo/akhq:0.14.1
runtime_folder        = {{ .topology.config.runtime_folder }}/akhq

# service config is auto-magically copied over in the service's runtime folder
runtime_config_folder = config
